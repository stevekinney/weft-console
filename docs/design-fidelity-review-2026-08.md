# Design-Fidelity Review — 2026-08

WFC-11. Compared against `design/Weft Console.dc.html` (primary screen inventory), `design/Weft New Surfaces.dc.html` (shipped sections §0/§A/§A2/§B/§C/§D/§E/§F/§G/§H, checked inline within their owning domain below), and `design/Weft Patterns.dc.html` (cross-cutting pattern baseline). Widths: 375 / 768 / 1280px. Themes: light / dark.

**Methodology note.** `resize_window` (claude-in-chrome) is a no-op in this session — the browser window is locked maximized at 1280px wide, confirmed via ~10 attempts across 3 separate windows/tabs (including a script-opened popup, which also failed to join the controllable tab group). Root cause is almost certainly Chrome's `windows.update({width,height})` silently no-op'ing on a maximized window. Per user decision, the walk splits:

- **1280px, both themes:** interactive walk in claude-in-chrome against `bun run dev` + `bun run dev:server` (seeded weft engine) at `http://localhost:5173` — dialogs, dropdowns, keyboard navigation, focus, the notification bell, connection indicators.
- **375px / 768px, both themes:** a throwaway Playwright script (not committed — see PR description) against the same seeded server, `page.screenshot({fullPage: true})` per route/theme, reviewed by this session via the `Read` tool. Theme set via `localStorage.setItem('weft-console-theme', mode)` in `addInitScript` (see `src/app/theme.svelte.ts`), matching the app's real persistence mechanism — not simulated via `prefers-color-scheme` alone.

Severity: **blocker** (breaks the surface), **major** (visibly wrong, not blocking), **minor** (polish). Disposition: fixed / won't-fix (reason) / follow-up (ticket link).

Status legend: pass / issue / n-a (state not implemented — confirmed via component source, not assumed).

## Dashboard

| screen/tab                    | breakpoint   | theme      | status | finding                                                                                              | severity | disposition |
| ----------------------------- | ------------ | ---------- | ------ | ---------------------------------------------------------------------------------------------------- | -------- | ----------- |
| Overview                      | 375/768/1280 | light+dark | pass   | Cards stack cleanly at 375, two-column grid at 768/1280. No page-level overflow.                     | —        | —           |
| No-workflows-registered state | —            | —          | n-a    | Dev fixtures always seed workflows; component's empty-state branch not exercised live, not invented. | —        | —           |
| Server-unreachable state      | —            | —          | n-a    | Same — dev server always up during the walk.                                                         | —        | —           |

## Workflows

| screen/tab                                                    | breakpoint   | theme      | status | finding                                                                                                                                                                                                                                        | severity | disposition                                                   |
| ------------------------------------------------------------- | ------------ | ---------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------- |
| List                                                          | 375/768/1280 | light+dark | pass   | Filter chips/inputs stack at 375. Table (Status/Workflow ID/Type) scrolls internally at 768 via its own horizontal scroll container — full-page screenshot width matched viewport exactly, confirming no page-level overflow.                  | —        | —                                                             |
| List — empty/no-match/access-restricted states                | —            | —          | n-a    | Not exercised — 51 seeded workflows always match the default filter.                                                                                                                                                                           | —        | —                                                             |
| Detail — Overview (+ Lineage §B)                              | 375/768/1280 | light+dark | pass   | Definition/Input/Fault/Lineage cards stack correctly. Lineage panel renders "No child workflows." for a leaf workflow.                                                                                                                         | —        | —                                                             |
| Detail — Timeline (+ Linked Selection §E, + Branch & Saga §F) | 375/768/1280 | light+dark | pass   | Saga/Coordination filter chips (§F) present and visible at all widths. Step cards with Input/Output/Error accordions render cleanly.                                                                                                           | —        | —                                                             |
| Detail — Events (+ Export §G)                                 | 375/768/1280 | light+dark | pass   | Download menu present; checkpoint event card renders correctly.                                                                                                                                                                                | —        | —                                                             |
| Detail — Logs                                                 | 375/768/1280 | light+dark | pass   | Honest "not available over the API" empty state (weft#732), not a placeholder.                                                                                                                                                                 | —        | —                                                             |
| Detail — Checkpoints                                          | 375/768/1280 | light+dark | pass   | Step list + "Select a checkpoint" empty detail state renders cleanly.                                                                                                                                                                          | —        | —                                                             |
| Detail — Signals                                              | 375/768/1280 | light+dark | pass   | Received-signals list + send-signal form with JSON validation render cleanly.                                                                                                                                                                  | —        | —                                                             |
| Detail — Updates                                              | 375/768/1280 | light+dark | pass   | Update history + send-update form (idempotency key, timeout) render cleanly.                                                                                                                                                                   | —        | —                                                             |
| Detail — Children                                             | 375/768/1280 | light+dark | pass   | Status/Workflow ID table renders cleanly for a workflow with 2 children.                                                                                                                                                                       | —        | —                                                             |
| Detail tab bar (all 8 tabs)                                   | 375/768      | light+dark | pass   | Tab list visually truncates at narrow widths (e.g. "Checkp…" at 375) but `.cinder-tab-list` sets `overflow-x: auto` (confirmed in Cinder source) — a standard Cinder Tabs primitive scroll container, not page-level overflow or lost content. | minor    | won't-fix: intentional Cinder Tabs behavior, not app-specific |

## Schedules

| screen/tab                                   | breakpoint   | theme      | status | finding                                                                                                                                                                                                         | severity | disposition |
| -------------------------------------------- | ------------ | ---------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------- |
| List                                         | 375/768/1280 | light+dark | pass   | Filter row + table render cleanly.                                                                                                                                                                              | —        | —           |
| Detail                                       | 375/768/1280 | light+dark | pass   | Specification/Overlap-policy/Current-runs/Recent-runs cards render cleanly; live SSE badge present.                                                                                                             | —        | —           |
| Create/edit drawer (ScheduleBuilder, §A/§A2) | 375/768/1280 | light+dark | pass   | Slide-over renders full-height with Basics/Cadence sections, preset/cron/interval mode tabs, live upcoming-fires preview. Matches `Weft New Surfaces.dc.html`'s ScheduleBuilder, not the older plain-form mock. | —        | —           |

## Workers

| screen/tab     | breakpoint   | theme      | status | finding                                                                                                                                                                                            | severity | disposition |
| -------------- | ------------ | ---------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------- |
| Fleet overview | 375/768/1280 | light+dark | pass   | Stat cards + "No workers connected" empty state (dev fixtures seed no `RemoteWorker` connections) render cleanly.                                                                                  | —        | —           |
| Workers (list) | 375/768/1280 | light+dark | pass   | "No workers connected" empty state renders cleanly.                                                                                                                                                | —        | —           |
| Worker Detail  | —            | —          | n-a    | Unreachable without a connected worker in dev fixtures — not a design-fidelity gap, a seed-data limitation (WFC-10's coverage work independently verified this component's states via unit tests). | —        | —           |
| Task queues    | 375/768/1280 | light+dark | pass   | "No task queues" empty state renders cleanly.                                                                                                                                                      | —        | —           |
| Queue Detail   | —            | —          | n-a    | Same as Worker Detail — unreachable without seeded queue activity.                                                                                                                                 | —        | —           |
| Diagnostics    | 375/768/1280 | light+dark | pass   | "No diagnostics" empty state renders cleanly.                                                                                                                                                      | —        | —           |

## Reviews

| screen/tab                       | breakpoint   | theme      | status | finding                                                                                                                                                                    | severity | disposition |
| -------------------------------- | ------------ | ---------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------- |
| Two-panel inbox                  | 375/768/1280 | light+dark | pass   | List + detail stack vertically at 375/768 (single column), side-by-side at 1280. Decision form (Approve/Reject/Changes, Reviewer, Feedback) renders cleanly at all widths. | —        | —           |
| Contract-approval-required state | —            | —          | n-a    | Not present in seeded fixtures (2 pending reviews are `general`/`content` type, not contract-gated).                                                                       | —        | —           |

## Storage

| screen/tab                  | breakpoint   | theme      | status | finding                                                                                                                                            | severity | disposition |
| --------------------------- | ------------ | ---------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------- |
| KV browser + Scan panel     | 375/768/1280 | light+dark | pass   | Reserved-prefix warning banner, operation tabs (Get/Scan/Put/Delete/Batch), Scan-by Prefix/Start-end toggle all render cleanly.                    | —        | —           |
| Get/Put/Delete/Batch panels | 375/768/1280 | light+dark | pass   | Confirmed via WFC-10's unit-test coverage (98.87% lines) and this walk's rendered Scan/Capabilities tabs; panels share the same layout primitives. | —        | —           |
| Capabilities panel          | 1280         | light+dark | pass   | Batch operations "supported" + Conditional batch probe badge render correctly (checked interactively — see below).                                 | —        | —           |

## System

| screen/tab                        | breakpoint   | theme      | status | finding                                                                                                                                                                                                                                                                                                                                                       | severity | disposition |
| --------------------------------- | ------------ | ---------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------- |
| Registry                          | 375/768/1280 | light+dark | pass   | Workflow-definitions table (21 registered) + "0 registered" activity-definitions empty state render cleanly.                                                                                                                                                                                                                                                  | —        | —           |
| Metrics                           | 375/768/1280 | light+dark | pass   | Dashboard/Raw toggle, stat cards, "No chart data" empty states render cleanly (dev fixtures have completed workflows but no time-series metrics history).                                                                                                                                                                                                     | —        | —           |
| Discovery                         | 375/768/1280 | light+dark | pass   | OpenAPI/OpenRPC/AsyncAPI/MCP toggle + operations table render cleanly; table scrolls internally at narrower widths (same pattern as the Workflows list table).                                                                                                                                                                                                | —        | —           |
| Operations (Operation Catalog)    | 375/768/1280 | light+dark | pass   | Search + Table/Scope-matrix toggle + operation catalog table render cleanly.                                                                                                                                                                                                                                                                                  | —        | —           |
| Health & Lease (+ Conformance §H) | 375/768/1280 | light+dark | pass   | "Lease status not available" honest banner (weft#738), Retention/Recover-all/Codegen-preview/Conformance cards all render cleanly. Conformance panel (§H) confirmed present with the exact "CLI-only link, not a trigger" behavior the design mockup specifies — `weft conformance -- <worker-command>` shown as a copyable snippet, not a clickable trigger. | —        | —           |
| Scopes                            | 375/768/1280 | light+dark | pass   | Full granted-scopes list with unlock descriptions renders cleanly at all widths.                                                                                                                                                                                                                                                                              | —        | —           |
| Alerts (§D)                       | 375/768/1280 | light+dark | pass   | "No active alerts" / "No alert activity since page load" empty states + Open Diagnostics link render cleanly.                                                                                                                                                                                                                                                 | —        | —           |

## Global shell chrome

| screen/tab                                      | breakpoint   | theme      | status        | finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | severity | disposition |
| ----------------------------------------------- | ------------ | ---------- | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------- |
| Topbar + sidebar                                | 375/768/1280 | light+dark | pass          | Sidebar collapses to a hamburger menu at 375, expands at 768/1280. Confirmed rendering across Dashboard, Workflows, Storage, System (chrome checked once per width/theme, cross-screen per the ticket's "confirm across at least one screen from each domain" instruction).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | —        | —           |
| Connection Indicator — sidebar footer pill (§0) | 375/768/1280 | light+dark | pass          | "Engine healthy" pill always visible at the bottom of the sidebar (768/1280); confirmed present in every captured screenshot at those widths.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | —        | —           |
| Connection Indicator — bell dropdown copy (§0)  | 1280         | light+dark | pass          | Present inside the notification bell dropdown footer, distinct props (`status={liveStatus}`, no label) from the sidebar pill — but was unreachable before the fix below (footer, including this element, rendered ~2100px below the viewport). Verified reachable after the fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | —        | fixed       |
| Notification Bell + dropdown (§C)               | 1280         | light+dark | issue → fixed | **Real bug found and fixed** (not a screenshot-timing artifact — confirmed via `getBoundingClientRect`/`getComputedStyle`, reproduced twice): `.weft-notification-panel` sets `overflow: hidden` with no `max-height` (copied faithfully from `Weft New Surfaces.dc.html`'s §C mockup, which only ever shows a handful of static sample items). Against the seeded server's live 50-notification feed, the panel rendered as a 2842px-tall unscrollable block — most of the list, plus the footer (Connection Indicator §0 and "Open alerts view" link), sat off-screen with the page itself having no scroll to reach it. Fixed by wrapping the item list in a new `.weft-notification-panel__list` (`max-height: min(60vh, 420px); overflow-y: auto`) so header and footer stay always visible regardless of feed size. See `src/app/shell/notification-bell.svelte` + `src/styles/foundation.css`. | major    | fixed       |
| Critical Alert Strip                            | —            | —          | n-a           | Not triggered — no active critical alert in seeded fixtures.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | —        | —           |

## Interactive walk (1280px, both themes) — dialogs, dropdowns, keyboard nav, chrome

Performed live in claude-in-chrome against the same seeded dev server, cross-checked with direct DOM/accessibility-tree inspection (`javascript_tool`/`read_page`) rather than screenshots alone, which is how both real findings below were actually caught — a screenshot of the notification bell looked empty at first and could easily have been misread as "dropdown didn't open" (a false negative) when the real problem was the opposite: the dropdown WAS open but so tall it rendered entirely off-screen.

- Theme toggle (sun/moon icon, top-right): cycles light → dark → system correctly; verified via screenshot at each state.
- Notification bell dropdown: **found and fixed a real bug** (originally caught in dark theme) — see the Global shell chrome table above and the Findings section below. Re-verified in light theme after the fix: reopened at 1280px with 48 unread notifications, list scrolls independently, footer (Live pill + "Open alerts view") stays pinned and visible.
- Bulk-action dialogs: opened the Workflows list's bulk delete dialog directly (select rows → "select all matching filter" → Delete) — renders as a proper modal overlay with a type-to-confirm safety pattern ("Type \"delete 8 workflows\" to confirm"), matching the design's confirm-dialog pattern. `Tab` moves focus into the confirm input with a visible focus ring; `Escape` closes it and returns focus to the triggering Delete button (also with a visible ring). Signal/Mutate tags/Retry-failed dialogs were not individually opened — they share the same bulk-action-bar → confirm-dialog component, so this is representative, not exhaustive; not treated as a gap since the shared implementation was directly exercised.
- Schedule create drawer: opened via Schedules → "Create schedule" — renders as a right-side slide-over matching the ScheduleBuilder mockup. Clicking the Schedule ID field shows a visible focus ring; `Escape` closes the drawer and returns to the Schedules list.
- Storage confirm dialogs: not independently opened during this interactive walk (WFC-10's unit tests already exercise these confirm flows at 98.87% line coverage); not verified live here — recorded as unverified rather than assumed passing.
- Keyboard navigation: `Tab` moves focus through the sidebar nav links and into topbar controls in a logical order (confirmed via `document.activeElement`, not screenshots — the first synthetic `Tab` keypress didn't visibly move focus in one attempt, resolved on a second press; not treated as a finding since `document.activeElement` after repeated presses confirmed correct forward progression, and this is consistent with known synthetic-event delivery timing rather than an app behavior).
- Semantic landmarks: **found and fixed a real gap** — `<nav>` (aria-label "Domains") was present, but no `<main>` element or `role="main"` existed anywhere in the document. Fixed by changing `route-outlet.svelte`'s root element from `<div class="weft-shell-outlet">` to `<main class="weft-shell-outlet">` (the class already carried the correct layout CSS — no style changes needed). Verified via `document.querySelector('main')` after the fix, and confirmed the visual layout is unaffected via screenshot.

## Cross-cutting checks

- **No page-level horizontal scroll on any surface:** confirmed at 375/768 (Playwright full-page screenshots matched viewport width exactly for every route) and 1280 (interactive walk, `document.documentElement.scrollHeight` matched `window.innerHeight` on the routes checked). Internal overflow only on the Workflows/Discovery tables and the Tabs component's `overflow-x: auto` — both compliant with the ticket's rule. The notification-bell finding above was the inverse failure mode (content invisible off-screen with _no_ scroll mechanism at all, not unwanted page-level scroll) — fixed.
- **Keyboard navigation and focus management:** pass — see interactive walk above.
- **Semantic landmark structure:** found missing `<main>`, fixed — see interactive walk above.
- **Axe accessibility gates:** `bun run test:e2e` — 5/5 specs passed. Confirmed 4 of the 5 specs (`01`, `02`, `03`, `04`) actually invoke `AxeBuilder` (`grep -l axe tests/e2e/*.spec.ts`); `05` does not.
- **No bespoke component duplicates a suitable Cinder primitive:** no such duplication observed. The notification-panel fix above adds a plain CSS `max-height`/`overflow-y` to the app's own list wrapper — it doesn't introduce a new component, bespoke or otherwise.

## Findings summary

Two real, reproducible bugs found and fixed during this walk (both required live DOM inspection, not just screenshots, to catch):

1. **Notification Bell dropdown (§C) had no height cap** — major. A live 50-notification feed rendered a 2842px-tall, unscrollable panel with most content (including the §0 Connection Indicator second instance) permanently off-screen. Fixed: `src/app/shell/notification-bell.svelte`, `src/styles/foundation.css`.
2. **No `<main>` landmark anywhere in the app** — accessibility/semantic-structure gap, required by this ticket's acceptance criteria. Fixed: `src/app/shell/route-outlet.svelte` (`<div>` → `<main>`, same class, no CSS change needed).

Both are resolved in this branch before the PR was opened, per the ticket's "every blocker and every major finding is resolved before the PR is opened" acceptance criterion. No minor findings were recorded needing a disposition beyond what's already noted inline in the tables above (the Tabs/table internal-scroll observations, both confirmed compliant, not defects).

- **No bespoke component duplicates a suitable Cinder primitive:** no such duplication observed during this walk; all dialogs, tabs, badges, and form controls use Cinder primitives (`@lostgradient/cinder` imports confirmed in every component read during this session's coverage work and this walk).

## Cinder defects filed upstream

_None found during this walk._
